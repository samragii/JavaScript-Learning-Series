
const scores = [40, 85, 65, 70, 30];

// Store last element's index
let lastIndex = scores.length-1;

// Loop from last to first index
for (let i=lastIndex;i>=0;i--) {

    // Display current element

   console.log(scores[i]);
}