// Replace ___ with your code

// Users data objects
let users = {
    user1: "Alice",
    user2: "Bob",
    user3: "Charlie"
};

// Retrieve all the keys
const newbox = Object.keys(users);

// Display keys length
console.log(newbox.length);