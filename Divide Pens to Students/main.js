// Create pen_number and student_number variables
let pen_number=14;
let student_number=3;

// Compute quotient
let quotient=Math.floor(pen_number/student_number);
console.log(quotient);

// Compute remainder
let reminder=pen_number%student_number;
console.log(reminder);