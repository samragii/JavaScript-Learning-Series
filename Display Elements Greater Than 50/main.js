// Array containing scores
const scores = [40, 85, 65, 70, 30];

// Loop through the array elements
// Display score if it's greater than 50
for(let i=0;i<scores.length-1;i++)
{
    if(scores[i]>50)
    {
        console.log(scores[i]);
    }
}