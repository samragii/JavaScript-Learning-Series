// Create an object with two key-value pairs
const car = {
    model:"Toyota",
    year:2022
};

// Access and display the model property
console.log(car.model);

// Access and display the year property
console.log(car.year);